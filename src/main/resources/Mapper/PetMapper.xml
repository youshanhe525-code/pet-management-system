<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pet_utf8.PetMapper">

    <select id="selectAll" resultType="pet_utf8.Pet">
        SELECT pid, pname, species, breed, sex, age, status FROM pet
    </select>

    <insert id="insert" parameterType="pet_utf8.Pet">
        INSERT INTO pet(pname, species, breed, sex, age, status)
        VALUES(#{pname}, #{species}, #{breed}, #{sex}, #{age}, #{status})
    </insert>

    <update id="update" parameterType="pet_utf8.Pet">
        UPDATE pet SET pname=#{pname}, species=#{species}, breed=#{breed},
        sex=#{sex}, age=#{age}, status=#{status}
        WHERE pid=#{pid}
    </update>

    <delete id="delete">
        DELETE FROM pet WHERE pid=#{pid}
    </delete>

</mapper>